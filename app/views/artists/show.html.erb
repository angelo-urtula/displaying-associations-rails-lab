<h1>List of <%= @artist.name %>'s Songs</h1>

<h3><%= pluralize(@artist.song_count, 'Song') %></h3>
<ul>
  <% @artist.songs.each do |p| %>
    <li><%= link_to "#{p.title}", song_path(p) %></li>
  <% end %>
</ul>