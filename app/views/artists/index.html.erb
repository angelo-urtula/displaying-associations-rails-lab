<h1>List of All Artists</h1>

<h3><%= pluralize(@artists.count, 'Artist') %></h3>
<ul>
  <% @artists.each do |p| %>
    <li><%= link_to "#{p.name} - #{pluralize(p.song_count, 'song')}", artist_path(p) %></li>
  <% end %>
</ul>